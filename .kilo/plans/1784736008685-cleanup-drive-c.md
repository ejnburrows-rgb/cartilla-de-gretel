# C: Drive Cleanup Plan

## Audit findings (2026-07-22)

### Downloads (biggest wins)
| Item | Size |
|------|------|
| `takeout-20260721T164133Z-1-001.zip` | ~450 MB |
| `takeout-20260721T164133Z-2-001.zip` | ~63 MB |
| 5 × `takeout-*.crdownload` (incomplete downloads) | ~473 MB |
| `Wispr Flow Setup-v1.6.7.exe` × 3 (identical, 349 MB each) | ~1.05 GB |
| `DevinUserSetup-x64-3.4.27.exe` | ~213 MB |
| `GitHubDesktopSetup-x64.exe` | ~308 MB |
| `Android-mic` installers × 2 | ~43 MB |
| `RobloxPlayerInstaller` × 2 | ~19 MB |
| `GoogleCloudSDKInstaller.exe` | ~2.5 MB |
| `Claude Setup.exe` | ~6.7 MB |
| Various zip bundles (stitch_*, Command-Center, etc.) | ~40 MB |

**Downloads total recoverable: ~2.6 GB**

### Temp (`%LOCALAPPDATA%\Temp`)
- Numerous stale `.tmp`, `.tmp.exe`, `.tmp.node` files from prior installers/uninstallers
- Dozens of orphaned `xml_file (*).xml` (0 bytes) cluttering the root
- Node native module bundles (~3.5 MB × 10 = ~35 MB)
- VSCode extension cache folder (`vscode-typescript`)
- `squirrel-uninstall.log`, `mat-debug-*.log`, various `.cpuprofile` dumps

**Temp recoverable: ~50–70 MB**

### npm cache (`%LOCALAPPDATA%\npm-cache`)
- Present, standard structure; size not quantified but typically safe to clean with `npm cache clean --force`

## Plan

1. **Delete stale Temp root files** — remove all `xml_file (*).xml`, old `.tmp` files older than 7 days, `.cpuprofile` files, Squirrel/mat debug logs
2. **Clean npm cache** — run `npm cache clean --force`; reclaim whatever is there
3. **Keep** VSCode-typescript and node-compile-cache folders under Temp (live caches)
4. **Archive or delete large Downloads installers** the owner no longer needs:
   - Keep or archive one copy of `Wispr Flow Setup`; delete the other two
   - Delete or archive `DevinUserSetup`, `GitHubDesktopSetup`, `android-mic`, `RobloxPlayerInstaller`, `GoogleCloudSDKInstaller`, `Claude Setup.exe` after confirming they are installed
5. **Delete incomplete `.crdownload` takeout files** — they are not valid zip archives
6. **Keep** `takeout-20260721T164133Z-*.zip` files (valid completed Google Takeout) — owner may need them
7. **Delete** duplicate zips (two `From Chaos to Command` PDFs, two `My-Build-Hub-Playbook` PDFs, duplicate stitch_* zips, etc.)
8. **Verify** folder sizes after each step

## Completed ✓

### Actions taken:
- Moved 24 files totaling ~3 GB to `D:\archive\downloads-backup\`
- Deleted `.cpuprofile` files
- Deleted `mat-debug-*.log` files
- Deleted `Squirrel-Uninstall.log`
- Deleted stale `*.xml` files
- Deleted `*.tmp.node` files
- Deleted `*.tmp.exe` files
- Ran `npm cache clean --force`

### Results:
| Metric | Before | After |
|--------|--------|-------|
| C: Used | 230.42 GB | 227.68 GB |
| C: Free | 6.70 GB | 9.44 GB |
| Reclaimed | - | ~3 GB |
