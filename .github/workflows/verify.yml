name: Verify Main

on:
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: verify-main
  cancel-in-progress: false

jobs:
  verify:
    uses: ./.github/workflows/cartilla-ci.yml
