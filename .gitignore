# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Operator-local production secrets (never commit — publishable URL/key dumps + teacher creds)
generated/prod-supabase-*.txt
generated/prod-teacher-creds*
generated/*.local.json
generated/vercel-*.env.local

# Restoration pipeline bulk output (scripts/restore-art.mjs). The mirrored
# restored art can be large; batch PRs (art/restore-<batch>) wire in the
# specific restored files they need. Committed proofs live in docs/restore-art-proofs/.
public/cartilla/art/restored/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/mcp.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Root-level generator output (source art lives in scratch/gretel-source-art/)
# Leading / anchors these patterns to repo root only — does not affect public/ or src/
/*.webp
/*.png
/*.pdf
*.pdf
.env*

# Playwright E2E output
/test-results/
/playwright-report/
tests/e2e/__screenshots__/
# local tooling / scratch (not project assets)
bin/
.kilo/
temp_slots.json
public/cartilla/art/test-overlays/
