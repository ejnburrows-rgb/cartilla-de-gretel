# GEMINI-NEEDED

None required for this pass for frames that had source crops.

Optional future (only if operator supplies source scans):

1. **pollito/gallina** closed-eye — need faithful crop file under `leccion-8-p/` first  
2. **serpiente** tongue-in micro — need faithful crop under `leccion-9-s/` first  

No Gemini generation was used to invent missing characters.
